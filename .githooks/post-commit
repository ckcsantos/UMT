#!/bin/sh
# Automatically push the current branch after each successful commit.
# This hook assumes origin is configured and the branch has an upstream.

branch=$(git symbolic-ref --short HEAD 2>/dev/null)
if [ -n "$branch" ]; then
  echo "Auto-pushing branch '$branch' to origin..."
  git push origin "$branch"
fi
