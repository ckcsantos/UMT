# Base URL for the backend API.
# Leave empty when using the Vite dev proxy (default for local dev).
# Set to the deployed API origin in staging/production, e.g.:
#   VITE_API_URL=https://api.umt.internal
VITE_API_URL=
